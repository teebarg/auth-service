log:
    level: DEBUG

http:
    routers:
        auth:
            rule: PathPrefix(`/`)
            service: auth

    services:
        auth:
            loadBalancer:
                servers:
                    - url: "http://auth:8000"
